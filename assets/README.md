# Assets

This folder stores the data used by the DriverHub installer.

Contents:

- `app-metadata.json`: app name, publisher, version, and logo settings
- `driver-manifest.json`: ordered install steps for the driver bundle
- `branding/`: logo and other branding assets
- `drivers/`: ADB, Fastboot, and Qualcomm driver payloads

